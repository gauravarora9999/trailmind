[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."adventure"]
  timeout = 30

[functions."adventure-plan"]
  timeout = 30

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
